{
  "mcpServers": {
    "mcp-atlassian": {
      "type": "local",
      "command": "node",
      "args": ["jira_mcp_server/index.js"],
      "tools": ["*"],
      "env": {
        "JIRA_URL": "${JIRA_URL}",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}"
      }
    }
  }
}
